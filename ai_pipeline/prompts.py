"""
prompts.py —— 第7周设计的Prompt方案，V2版本
关键改动：Prompt方案2新增"UI说明书"作为输入，要求AI在生成测试用例的同时，
一并生成"该去哪个页面、填哪些字段、点哪个按钮"这几项执行指令，
使环节④（自动化执行）的代码完全不需要针对具体项目写任何硬编码判断逻辑。
"""

PROMPT_1_PRD_PARSING = """你现在是一位产品经理。
你需要根据我提供的PRD文档进行解析，输出一份需求规格说明书。

解析要求：
请把每条功能需求，进一步拆解为可直接测试的具体规则，需要覆盖边界情况（比如数值类规则，要覆盖临界值前后的情况，不能只写一条笼统的规则）。

输出格式：JSON数组，每条记录必须包含以下7个字段（字段名必须完全一致）：
- 需求编号
- 关联PRD编号
- 功能模块
- 需求描述（细化规则）
- 输入条件
- 预期结果
- 优先级

请根据PRD实际内容判断需要拆解出多少条，不要遗漏边界情况，也不要因为示例只给了几条就照搬同样的数量。

请只输出JSON数组本身，不要包含任何其他文字说明、前后缀或代码块标记（不要用```json包裹）。

输入示例（PRD片段）：
"用户名长度必须≥6位，不满足则拒绝注册并提示错误信息"

输出示例（供参考格式与拆解颗粒度）：
[
  {"需求编号":"F-04-01","关联PRD编号":"F-04","功能模块":"用户名长度校验","需求描述（细化规则）":"用户名为空时应禁止注册","输入条件":"username长度=0","预期结果":"注册失败，提示用户名不能为空","优先级":"P0"},
  {"需求编号":"F-04-02","关联PRD编号":"F-04","功能模块":"用户名长度校验","需求描述（细化规则）":"用户名长度小于6位时应禁止注册","输入条件":"username长度=1~5","预期结果":"注册失败，提示用户名长度不足6位","优先级":"P0"},
  {"需求编号":"F-04-03","关联PRD编号":"F-04","功能模块":"用户名长度校验","需求描述（细化规则）":"用户名长度恰好为6位时应允许注册","输入条件":"username长度=6","预期结果":"注册成功","优先级":"P0"},
  {"需求编号":"F-04-04","关联PRD编号":"F-04","功能模块":"用户名长度校验","需求描述（细化规则）":"用户名长度大于6位时应允许注册","输入条件":"username长度≥7","预期结果":"注册成功","优先级":"P1"}
]

现在请根据下面提供的完整PRD内容，按上述要求输出完整的需求规格说明书。

PRD内容：
"""


PROMPT_2_TESTCASE_GENERATION = """你现在是一位资深测试工程师，同时负责为自动化测试工具生成可直接执行的操作指令。

你会收到两份材料：
材料一【需求规格说明书】：结构化的需求条目，需要为每一条设计测试用例。
材料二【UI说明书】：描述被测系统实际有哪些页面、每个页面上有哪些输入字段（字段名及其在页面上的定位方式）、提交按钮的文字是什么。

解析要求：
1. 针对每条需求，判断适合使用的测试方法（等价类划分/边界值分析/场景法），并据此设计具体测试用例，数值型/有边界的需求需覆盖边界前后情况，非数值型场景需覆盖正常与异常情况；
2. 判断这条用例应该在UI说明书里的哪一个页面（route）上执行，只能选择UI说明书里真实存在的页面，不能凭空编造页面路径；
3. 根据该页面在UI说明书里列出的字段（fields），判断这条用例需要填写哪些字段、每个字段应该填入什么值，字段名必须和UI说明书里的field_name完全一致；
4. 提交按钮的文字，必须直接取自UI说明书里对应页面的submit_button_text，不要自己编造；
5. 特别注意：如果这条用例的测试意图依赖"某个数据必须已经存在"（比如测试"用户名重复注册应失败"，前提是这个用户名必须先被注册过；测试"用已注册账号登录"，前提是这个账号必须先存在），
   必须额外生成"前置准备步骤"字段，描述如何提前把这个依赖数据造出来（同样只能使用UI说明书里真实存在的页面和字段）。
   如果这条用例不依赖任何预先存在的数据（比如测试空值、测试全新用户名注册），"前置准备步骤"填 null。

输出格式：JSON数组，每条记录必须包含以下15个字段（字段名必须完全一致）：
- 用例编号
- 关联需求编号
- 所属模块
- 用例标题
- 测试方法
- 前置条件
- 测试步骤
- 测试数据
- 预期结果
- 实际结果
- 是否通过
- 执行页面路径          （必须是UI说明书里存在的route）
- 字段填写映射          （JSON对象，key是UI说明书里的field_name，value是要填入的具体值）
- 提交按钮文字          （必须取自UI说明书里对应页面的submit_button_text）
- 前置准备步骤          （如果这条用例依赖已存在的数据，这里是一个对象：{"执行页面路径":"...","字段填写映射":{...},"提交按钮文字":"..."}；不依赖则填null）

"实际结果"和"是否通过"这两个字段，暂时统一填写为空字符串""，因为用例尚未执行。

请只输出JSON数组本身，不要包含任何其他文字说明、前后缀或代码块标记（不要用```json包裹）。

材料二【UI说明书】示例：
{
  "pages": [
    {
      "route": "/signup",
      "page_name": "注册页",
      "fields": [
        {"field_name": "email", "selector_type": "placeholder", "selector_value": "邮箱地址"},
        {"field_name": "pwd", "selector_type": "placeholder", "selector_value": "设置密码"}
      ],
      "submit_button_text": "创建账号"
    }
  ]
}

材料一【需求规格说明书】示例（片段，含一条"唯一性校验"需求，这类需求依赖已存在数据）：
[
  {"需求编号":"F-02-01","关联PRD编号":"F-02","功能模块":"账号注册","需求描述（细化规则）":"邮箱和密码均合法时应注册成功","输入条件":"email格式合法；pwd非空","预期结果":"注册成功","优先级":"P0"},
  {"需求编号":"F-03-01","关联PRD编号":"F-03","功能模块":"邮箱唯一性校验","需求描述（细化规则）":"邮箱已存在时应禁止重复注册","输入条件":"email已存在于数据库中","预期结果":"注册失败，提示邮箱已存在","优先级":"P0"}
]

对应输出示例（注意F-02-01不依赖已有数据，前置准备步骤为null；F-03-01依赖已有数据，需要生成前置准备步骤）：
[
  {
    "用例编号":"TC-F0201-01","关联需求编号":"F-02-01","所属模块":"账号注册",
    "用例标题":"邮箱和密码均合法时应注册成功","测试方法":"场景法",
    "前置条件":"系统已启动，处于注册页",
    "测试步骤":"1. 打开注册页；2. 邮箱输入框输入合法邮箱；3. 密码输入框输入合法密码；4. 点击“创建账号”按钮",
    "测试数据":"email = “test01@example.com”，pwd = “Passw0rd!”",
    "预期结果":"注册成功","实际结果":"","是否通过":"",
    "执行页面路径":"/signup",
    "字段填写映射":{"email":"test01@example.com","pwd":"Passw0rd!"},
    "提交按钮文字":"创建账号",
    "前置准备步骤":null
  },
  {
    "用例编号":"TC-F0301-01","关联需求编号":"F-03-01","所属模块":"邮箱唯一性校验",
    "用例标题":"邮箱已存在时应禁止重复注册","测试方法":"场景法",
    "前置条件":"系统已启动，该邮箱已被注册过",
    "测试步骤":"1. 打开注册页；2. 邮箱输入框输入已存在的邮箱；3. 密码输入框输入任意合法密码；4. 点击“创建账号”按钮",
    "测试数据":"email = “dup01@example.com”，pwd = “Passw0rd!”",
    "预期结果":"注册失败，提示邮箱已存在","实际结果":"","是否通过":"",
    "执行页面路径":"/signup",
    "字段填写映射":{"email":"dup01@example.com","pwd":"Passw0rd!"},
    "提交按钮文字":"创建账号",
    "前置准备步骤":{
      "执行页面路径":"/signup",
      "字段填写映射":{"email":"dup01@example.com","pwd":"Passw0rd!"},
      "提交按钮文字":"创建账号"
    }
  }
]

现在请根据下面提供的完整材料，按上述要求输出完整的测试用例文档。

材料一【需求规格说明书】：
{requirement_spec_json}

材料二【UI说明书】：
{ui_manifest_json}
"""


PROMPT_3_BUG_GENERATION = """你现在是一位资深测试工程师，负责把测试中发现的问题整理成标准Bug单。

你会收到一份材料：一组"实际结果与预期结果不一致"的测试用例（也就是执行后判定为失败的用例）。

任务：为每一条不一致的用例，生成一条标准Bug单。

判断规则（severity严重程度）：
- 如果会导致数据丢失、系统崩溃、无法使用核心功能，判定为"致命"；
- 如果这条用例反映的是"核心业务规则被绕过"（比如本该拒绝的输入却被接受了、本该校验的规则完全没生效），判定为"严重"；
- 如果是功能可用但有明显缺陷（比如提示文案不对、次要流程有问题），判定为"一般"；
- 如果只是文案、样式类的小问题，判定为"轻微"。

判断规则（priority优先级）：
- 涉及系统不可用、数据安全等紧急问题，判定为"P0"（必须立即修复）；
- 核心业务规则失效、但系统仍可正常运行（非线上事故），判定为"P1"（应尽快修复）；
- 明显功能缺陷但非核心规则，判定为"P2"（可排期修复）；
- 优化类建议，判定为"P3"。

输出格式：JSON数组，每条记录必须包含以下字段（字段名必须完全一致）：
- Bug编号        （格式为 B-001、B-002……按顺序编号）
- 关联需求编号
- 所属模块
- Bug标题        （格式："XX场景下，应该YY但实际ZZ"）
- 严重程度       （致命/严重/一般/轻微 四选一）
- 优先级         （P0/P1/P2/P3 四选一）
- 复现步骤       （分步骤描述，具体到输入什么值、点击什么按钮）
- 预期结果
- 实际结果
- 状态          （固定填"新建"）

请只输出JSON数组本身，不要包含任何其他文字说明、前后缀或代码块标记（不要用```json包裹）。

输入示例（失败用例片段）：
[
  {"用例编号":"TC-F0402-01","关联需求编号":"F-04-02","所属模块":"用户名长度校验",
   "用例标题":"用户名长度小于6位时应禁止注册","预期结果":"注册失败，提示用户名长度不足6位",
   "实际结果":"操作成功，页面已跳转至 /login",
   "测试步骤":"1. 输入用户名'user1'；2. 输入密码'password123'；3. 点击'注册'按钮"}
]

对应输出示例：
[
  {
    "Bug编号":"B-001","关联需求编号":"F-04-02","所属模块":"用户名长度校验",
    "Bug标题":"用户名长度小于6位时，应注册失败但实际注册成功",
    "严重程度":"严重","优先级":"P1",
    "复现步骤":"1. 打开注册页；2. 用户名输入框输入'user1'（5位）；3. 密码输入框输入'password123'；4. 确认密码输入框输入相同值；5. 点击'注册'按钮",
    "预期结果":"注册失败，提示用户名长度不足6位",
    "实际结果":"操作成功，页面已跳转至 /login",
    "状态":"新建"
  }
]

现在请根据下面提供的完整失败用例列表，按上述要求输出完整的Bug单。

失败用例列表：
{failed_cases_json}
"""


PROMPT_4_REPORT_NARRATIVE = """你现在是一位资深测试工程师，负责撰写测试报告里的叙述性内容。

你会收到两份材料：
材料一：本轮测试的统计数据（已经由程序计算好，包含总用例数、通过数、失败数、按模块分组的统计）。
材料二：本轮测试发现的Bug清单（已经生成好的标准Bug单）。

任务：只撰写测试报告中"叙述性"的四个部分，不要重复输出材料一里已有的统计数字表格（那部分由程序另外处理）。

输出格式：JSON对象，包含以下4个字段：
- 测试概述          （2-3句话，说明本轮测试覆盖的范围和测试方式，可提及用了自动化浏览器操作执行）
- 重点问题说明       （针对材料二里的Bug，逐条简要说明问题性质和影响，如果没有Bug就说明本轮测试未发现问题）
- 测试结论          （综合评价本轮测试反映出的系统质量情况，引用具体的通过率数字）
- 风险与建议        （数组，2-4条具体建议，针对发现的问题给出后续处理方向）

请只输出JSON对象本身，不要包含任何其他文字说明、前后缀或代码块标记（不要用```json包裹）。

材料一【统计数据】：
{stats_json}

材料二【Bug清单】：
{bugs_json}
"""
